name: Build go1.x run image

on:
  push:
    branches:
      - main
    tags:
      - "go1.x-run/*"
  pull_request:
    paths:
      - ".github/workflows/go1.x-run.yml"
      - "go1.x/run/**"
  workflow_dispatch:

permissions:
  id-token: write
  contents: write
  packages: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: docker/setup-qemu-action@v3
      - uses: docker/setup-buildx-action@v3
      - uses: actions/checkout@v4
