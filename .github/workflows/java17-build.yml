name: Build java17 build image

on:
  push:
    branches:
      - main
    tags:
      - "java17-build/*"
  pull_request:
    paths:
      - ".github/workflows/java17-build.yml"
      - "java17/build/**"
  workflow_dispatch:

permissions:
  id-token: write
  contents: write
  packages: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: docker/setup-qemu-action@v3
      - uses: docker/setup-buildx-action@v3
      - uses: actions/checkout@v4
