name: Build base.al2023 run image

on:
  push:
    branches:
      - main
    tags:
      - "base.al2023-run/*"
  pull_request:
    paths:
      - ".github/workflows/base.al2023-run.yml"
      - "base.al2023/run/**"
  workflow_dispatch:

permissions:
  id-token: write
  contents: write
  packages: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: docker/setup-qemu-action@v3
      - uses: docker/setup-buildx-action@v3
      - uses: actions/checkout@v4
