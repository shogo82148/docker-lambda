name: Build java11 run image

on:
  push:
    branches:
      - main
    tags:
      - "java11-run/*"
  pull_request:
    paths:
      - ".github/workflows/java11-run.yml"
      - "java11/run/**"
  workflow_dispatch:

permissions:
  id-token: write
  contents: write
  packages: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: docker/setup-qemu-action@v3
      - uses: docker/setup-buildx-action@v3
      - uses: actions/checkout@v4
