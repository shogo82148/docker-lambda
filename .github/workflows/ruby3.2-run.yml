name: Build ruby3.2 run image

on:
  push:
    branches:
      - main
    tags:
      - "ruby3.2-run/*"
  pull_request:
    paths:
      - ".github/workflows/ruby3.2-run.yml"
      - "ruby3.2/run/**"
  workflow_dispatch:

permissions:
  id-token: write
  contents: write
  packages: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: docker/setup-qemu-action@v3
      - uses: docker/setup-buildx-action@v3
      - uses: actions/checkout@v4
