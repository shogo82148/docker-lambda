name: Build base.al2023 build image

on:
  push:
    branches:
      - main
    tags:
      - "base.al2023-build/*"
  pull_request:
    paths:
      - ".github/workflows/base.al2023-build.yml"
      - "base.al2023/build/**"
  workflow_dispatch:

permissions:
  id-token: write
  contents: write
  packages: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: docker/setup-qemu-action@v3
      - uses: docker/setup-buildx-action@v3
      - uses: actions/checkout@v4
